let alturacent = 182
let alturametros = 1.82
let peso = 88.5
let altredo= alturametros.toFixed(0)
let pesoredon= Math.floor(peso)
console.log(pesoredon)
let max= "el máximo valor que se puede obtener en Javascript + 1 es "+ Number.MAX_VALUE
